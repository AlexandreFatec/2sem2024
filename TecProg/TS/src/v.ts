export default class Veiculo {
    public nome: string;

    constructor(nome: string) { //constructor é basicamente para inicializar um objeto, constructor método especial, qualquer funcao criada dentro de uma classe é denominada de método
        this.nome = nome;
    }
}